import Club from '../db/models/club';
import { Request, Response, NextFunction } from 'express';

const getAllClubs = (req: Request, res: Response, next: NextFunction) => {
  //
};

const getClub = (req: Request, res: Response, next: NextFunction) => {
  //
};

const create_club = (req: Request, res: Response, next: NextFunction) => {
  //
};

const update_club = (req: Request, res: Response, next: NextFunction) => {
  //
};

const delete_club = (req: Request, res: Response, next: NextFunction) => {
  //
};

export { getAllClubs, getClub, create_club, update_club, delete_club };
